{% extends "backoffice.html.twig" %}


{% block body %}


{{ form(form) }}


<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Titre</th>
      <th scope="col">Actions</th>
    </tr>
  </thead>
  <tbody>

{% for categorie in categories %}

    <tr>
      <th scope="row">{{ loop.index }}</th>
      <td>{{ categorie.titre }}</td>
      <td>
      <a href="{{ path('modificationCategorie', { id : categorie.id }) }}" class="btn btn-success">Modifier</a>
  <a href="{{ path('suppressionCategorie', { id : categorie.id }) }}" onclick="return confirm('Etes vous sûr de vouloir supprimer ce categorie?');" class="btn btn-danger">Supprimer</a>
      
      </td>
    </tr>
{% endfor %}

  </tbody>
</table>




{% endblock %}